{
  "name": "unisa-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "check": "npm run test && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.24.3"
  }
}
